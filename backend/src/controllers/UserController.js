const State = require('../models/State');

module.exports = {
    info: async(req, res) => {
        res.json({states});
    },
    editAction: async(req, res) => {
        
    },
    getStates: async(req, res) => {
        
    }
}